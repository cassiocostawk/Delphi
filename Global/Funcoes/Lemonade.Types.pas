unit Lemonade.Types;

interface

type
  TReportRecords = (rrAll, rrCurrent);
  TSaveResult    = (srOk, srEmpty, srError);
  TStateLogin    = (slDadosIncorretos, slSemPermissao, slLoginOk, slErro);
  TTextPosition  = (tpBegin, tpEnd);
  TScreenAlign   = (saTop, saBottom, saLeft, saRight, saCenter, saNone);

implementation

end.
